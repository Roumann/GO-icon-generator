package generate

import (
	"fmt"
)


func AppIcons(filePath string, padding float32, bgColor string) {

	// create a background with specified color and size
	// resize the icon to the required size minus padding
	// combine the icon and the background and center it
	// save the final image


	

	fmt.Println("\n🎉 App icons generated successfully. ")
	fmt.Println("")
}
